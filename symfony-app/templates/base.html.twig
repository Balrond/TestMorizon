<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}Welcome!{% endblock %}</title>

    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}
        {# Bootstrap CSS #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

        {# Bootstrap Icons (ładne ikonki bez dodatkowych bibliotek) #}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

        {# Delikatne ulepszenia wyglądu #}
        <style>
            body { background: #f6f7fb; }
            .app-shell { min-height: 100vh; display: flex; flex-direction: column; }
            main { flex: 1; }
            .card { border: 0; box-shadow: 0 6px 22px rgba(16, 24, 40, .06); border-radius: 14px; }
            .table thead th { font-weight: 600; }
            .btn { border-radius: 10px; }
            .btn-group .btn { border-radius: 10px; }
            .navbar { box-shadow: 0 6px 22px rgba(16, 24, 40, .06); }
            .table-responsive { overflow: visible; }
        </style>
    {% endblock %}
</head>

<body>
<div class="app-shell">

    <nav class="navbar navbar-expand-lg bg-white">
        <div class="container py-1">
            <a class="navbar-brand fw-semibold d-flex align-items-center gap-2" href="{{ path('users_index') }}">
                <i class="bi bi-people"></i>
                <span>Users App</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#topNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="topNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('users_index') }}">
                            <i class="bi bi-list-ul me-1"></i>Users
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('users_new') }}">
                            <i class="bi bi-plus-circle me-1"></i>New user
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container py-4">
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ type == 'error' ? 'danger' : type }} alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
        {% endfor %}

        {% block body %}{% endblock %}
    </main>

    <footer class="py-4">
        <div class="container text-center text-muted small">
            <span>© {{ "now"|date("Y") }} Users App</span>
        </div>
    </footer>

</div>

{% block javascripts %}
    {# Bootstrap JS bundle (modal/offcanvas/dropdown działa) #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}
</body>
</html>
